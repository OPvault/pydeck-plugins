## 1.0.4 — 2026-08-29

- Restored the folder, profile and plugin-id imports that the pydeck lib/ repackaging moved, so Enter Folder, Return Folder and Switch Profile load again instead of failing at import.
